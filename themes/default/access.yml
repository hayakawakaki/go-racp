# Per-theme access config. Operator owns this file.
# Contains only ThemePages.* entries (other groups belong in root access.yml).

ThemePages:
  Index:
    Roles: ["Public"]
  ServerInfo:
    Roles: ["Public"]
  Download:
    Roles: ["Public"]
