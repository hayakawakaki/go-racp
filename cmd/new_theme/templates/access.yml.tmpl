# Contains only ThemePages.* entries (other groups belong in main access.yml).
# Add one entry per page under themes/{{.Name}}/pages/.
#
# Examples:
#   MyPage:
#     Roles: ["Public"]
#   ModeratorPage:
#     Roles: ["Moderator"]

ThemePages:
